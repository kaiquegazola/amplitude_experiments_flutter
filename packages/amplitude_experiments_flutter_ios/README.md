# amplitude_experiments_flutter_ios

The iOS implementation of [`amplitude_experiments_flutter`](https://pub.dev/packages/amplitude_experiments_flutter).

## Usage

This package is automatically included in your app when you use `amplitude_experiments_flutter`. You should not need to add it to your `pubspec.yaml`.

## Requirements

- iOS 13.0+
- CocoaPods
