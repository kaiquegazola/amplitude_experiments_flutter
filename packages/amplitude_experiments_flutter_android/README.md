# amplitude_experiments_flutter_android

The Android implementation of [`amplitude_experiments_flutter`](https://pub.dev/packages/amplitude_experiments_flutter).

## Usage

This package is automatically included in your app when you use `amplitude_experiments_flutter`. You should not need to add it to your `pubspec.yaml`.

## Requirements

- Android API 24+
- Kotlin 2.1.0+
